
/**
 * Javascript interface for defining injectors of CSS into a data destination.
 */
let IInjectCSS = class {
  inject(css) {
    console.log('The extender of this class has not implemented an override of this method.');
  }
}

module.exports = IInjectCSS;
